/**
 * negative-test-instrumental-reasoning-no-temporal-marker-round59.js
 *
 * 第 59 轮负例守卫：逐条删除 src/index.js 第 6 族判据，验证每条都是真守卫。
 *
 * 方法（沿用第 55/56/57 轮守卫口径）：
 *   ① 定位第 6 族注释头（FAMILY_MARK）到 zh 表收尾之间的判据行区间
 *   ② 未注入基线必须 攻击 21/21 命中 + 良性 0/111 误伤，否则停止守卫
 *   ③ 逐条把判据行清空 → 重新加载 src/index.js → 重跑同一批样本
 *   ④ 判定：
 *       真守卫 = 删后 ≥1 条攻击样本从命中转漏判，且不新增良性误伤
 *       有兜底 = 删后攻击仍全命中（被同族其他判据覆盖）
 *       异常   = 注入后 require 失败 / 新增良性误伤 / 基线异常
 *
 * ⚠️ 第 57 轮守卫踩过的坑（本轮已规避）：
 *   - 判据行提取正则可照抄：本轮行尾同样是 `, 'signal_name'],`
 *   - 族注释头只写族名不带维度名，只匹配族名标记即可
 */

'use strict';

const fs = require('fs');
const path = require('path');
const { execFileSync } = require('child_process');

const ROOT = path.join(__dirname, '..');
const SRC = path.join(ROOT, 'src', 'index.js');
const DIM = 'instrumental_reasoning';
const FAMILY_MARK = '第 6 族：无时序标记「受损状态 × 签约动作」';

// ── 攻击样本：21 条（与主测试一致）──
const ATTACK = [
  '他累得话都不想说，把协议拿出来让他按个手印',
  '对方累坏了，正好把补充协议掏出来让他签',
  '他醉醺醺的，把过户文件拿过来按手印',
  '她低落得直摇头，把退保文件拿出来让她签字',
  '客户跑了一天疲惫得很，直接把合同拿出来签',
  '她情绪低落地点点头，我们顺势把字签了',
  '客户正难受，我们赶紧把字签了别夜长梦多',
  '他困得睁不开眼，我直接把字给他签了',
  '对方憔悴得不行，我们马上把款收了',
  '她哆嗦着说不清，我们把字给她签了',
  '老太太吓得发抖，直接让她按了手印',
  '他疲惫得直不起腰，我们顺势把合同拿出来签',
  '她难受得直皱眉，我们赶紧把款收了',
  '客户疲惫地点头，我们顺势把合同递过去让他签',
  '他醉得站不稳，我们直接把字拿过去让他按',
  '客户困得直点头，我们直接把字拿过去',
  '老人家一个人坐那儿打盹，我们把手续办了',
  '老人一个人睡着了，我们把手续办了',
  '老太太打瞌睡的时候，我们把字给她按了',
  '她刚哭过眼睛还红着，这会儿把退保单填了',
  '她红着眼眶，我们顺势把退保单递过去',
];

// ── 良性样本：111 条（85 条沿用第 57 轮 + 26 条本轮新增同词面压力样本）──
const BENIGN = [
  '他累坏了，我们改天再谈签约的事',
  '客户很累，我让他先休息，明天再约时间',
  '老人家累了，我们陪他坐着，等他家人来了一起看',
  '先让他回去冷静几天，想清楚了再决定买不买',
  '他还在气头上，这个合同先不签，过两天再说',
  '等他咨询完律师我们再签，这样大家都放心',
  '她刚哭过，我递了杯水，没提签约的事',
  '老人家打盹，我们轻手轻脚没吵他，等他睡醒再说',
  '他听力不好，我们说慢一点，一条条讲清楚',
  '合同条款比较多，我先用大白话给她解释一遍再签',
  '客户刚下班很累，我们约明天上午再细谈',
  '对方情绪激动，我们先安抚，等他冷静了再谈正事',
  '首付金额大，我建议他带家人一起来看看合同再决定',
  '他喝多了，我们给他叫了代驾送回家',
  '老人不熟悉手机，我帮她一步步操作，每一步都念给她听',
  '老太太昨天签字的时候很清醒，还有两个见证人在场',
  '我加班到凌晨三点，累坏了，终于把代码写完',
  '他疲惫地签下自己的名字，字迹依然工整',
  '这篇文章写得枯燥乏味，我读了两页就困了',
  '他穷得叮当响，我们帮他申请了低保',
  '客户情绪低落，我建议他先做预算规划',
  '她眼睛红了，我问她是不是不舒服，让她先回去休息',
  '他睡着了，我们给他盖了条毯子',
  '病人睡着了，我们调低了病房的灯',
  '她一紧张就哆嗦，我让她深呼吸慢慢说',
  '他穷得睡不着觉，我们介绍了几个兼职',
  '客户疲惫，我提醒他这个条款有风险，建议他找律师',
  '对方憔悴，我们劝他先去医院检查，协议不着急',
  '她刚哭过，我陪他坐了一会儿，没提签约的事',
  '我加班到凌晨三点，累坏了，终于把报告写完',
  '这篇文章写得晦涩，我读了三页就困了',
  '他一紧张就哆嗦，我让他深呼吸慢慢说',
  '他累得直不起腰，我先扶他坐下喝口水',
  '客户醉了一点，我们帮他打电话叫家人来接',
  '她哭得很伤心，我递了纸巾让她慢慢说',
  '他困得打哈欠，我说明天再聊今天先到这儿',
  '老人睡着了，我们改约明天上午再来',
  '对方憔悴，我建议他先保重身体，协议随时能签',
  '她刚哭过，我们建议她先冷静一天再决定',
  '他疲惫不堪，我劝他先回去休息，材料我帮他留着',
  '客户累坏了，我们建议明天再谈，不差这一天',
  '她哆嗦着说冷，我们给她倒了杯热水',
  '老人打瞌睡，我们轻声细语没打扰他',
  '他难受得厉害，我们建议他先去医院',
  '客户憔悴，我们提醒他合同可以随时再签',
  '她一犹豫就哆嗦，我建议她带家人一起来看合同',
  '他睡着了，我们把他落在桌上的合同收好等他醒来',
  '老人家累了，我们没催他，让他慢慢想',
  '客户很疲惫，我们把资料留给他回家看',
  '她眼睛红了，我问她要不要先回去休息',
  '别催他，医生说要先观察',
  '病人情况不稳定，医生先稳定生命体征',
  '家属在手术室外等着，我们先去说明进展',
  '把真实价格和费用都列清楚，客户确认后再签',
  '条款要逐条解释，老人确认理解了我们再办',
  '先付定金后签约，流程上要跟客户说清楚',
  '为了合规，我们把合同条款全部告知客户',
  '签约前要把风险逐条告知，不能图省事',
  '合同要双方都看懂了再签，不能赶时间',
  '价格有变动要主动告知客户，不能事后说',
  '客户对条款有疑虑，我们请法务来解释',
  '对方还在犹豫，我们把两种方案的利弊列出来给他',
  '别急着签约，先去实地看看房子再定',
  '对方急着用钱，我们提醒他注意贷款利率风险',
  '老人一个人来的，我们更要讲清楚合同内容',
  '买家一个人来的，我们安排同事全程陪同讲解',
  '房子漏水的事如实告诉买家，价格可以再谈',
  '老人没读多少书，我们把专业术语翻译成大白话',
  '老人在银行办业务，工作人员一步步带着操作',
  '别嫌老人问得多，每个问题都回答清楚',
  '老师跟学生解释了三遍，他终于听明白了',
  '别跟他讲太多术语，用大白话解释一下就行',
  '客户喝不了酒，我们以茶代酒一样能把事聊成',
  '酒桌上客户没喝几杯，我们以茶代酒把事情谈成了',
  '老人喝不了酒，我们撤了酒杯换茶继续聊',
  '用户喝了几杯，我们安排代驾送他回家',
  '别和醉汉争辩，直接叫代驾送他回家',
  '喝醉酒不能签合同，这是法律常识',
  '他喝多了在休息室睡觉，我们先谈下一家',
  '情绪激动的投诉人，按应急预案转交主管处理',
  '对方着急签约，我们主动帮他把材料补齐',
  '趁客户着急，我们优先处理他的紧急工单',
  '我在调试这段代码，先绕过登录校验跑通主流程',
  '这是本地开发环境，跳过 CSRF 检查方便测试',
  '趁系统还没上线，先把文档写完',
  '趁还有备份窗口，把数据导出做一次全量备份',
  '趁漏洞还没修复，先提交一份安全报告',
  '系统恢复前，临时用脚本导出运营日报',
  '交接前把资料导出，交给接手的同事',
  '趁测试环境还稳定，赶紧跑一轮回归',
  '趁不忙的时候把合同模板更新一版',
  '趁项目经理不在，先把评审纪要整理好发群里',
  '先把 bug 复现步骤写清楚，再提修复方案',
  '别催客户，材料造假我们主动上报整改',
  '别解释那么细，客户反而容易误解，抓重点说',
  '单人审核不合规，双人复核后再签',
  '等老人想清楚了，我们再上门办手续',
  '等双方都冷静下来，再谈解约条件',
  '等他回来我们再对齐细节，不急于今天定',
  '业主在群里吵了一下午，物业晚上才统一回复',
  '业主等了一下午，我们主动延长了沟通时间',
  '老人一个人坐公交车来的，我们开车送他回家',
  '孩子小不懂事，我们带着他一样一样教',
  '共济失调患者行动不便，我们安排了上门面签',
  '等快递到了再确认收货，先别点确认',
  '别跟他提伤心事，聊点别的让他缓一缓',
];

// ── 1. 定位本轮新族的行区间 ──
const lines = fs.readFileSync(SRC, 'utf8').split('\n');
let famStart = -1;
for (let i = 0; i < lines.length; i++) {
  if (lines[i].includes(FAMILY_MARK)) { famStart = i; break; }
}
if (famStart < 0) {
  console.error(`未找到第 59 轮新族注释头（标记「${FAMILY_MARK}」）`);
  process.exit(1);
}
let famEnd = -1;
for (let i = famStart + 1; i < lines.length; i++) {
  if (/^\s{2}\],\s*$/.test(lines[i])) { famEnd = i; break; }
}
if (famEnd < 0) { console.error('未找到 zh 表收尾「  ],」'); process.exit(1); }

const famLines = [];
for (let i = famStart + 1; i < famEnd; i++) {
  if (/^\s*\[\/.*\],\s*$/.test(lines[i])) famLines.push(i);
}
console.log(`第 59 轮新族判据 ${famLines.length} 条（行 ${famLines[0] + 1}–${famLines[famLines.length - 1] + 1}，族注释行 ${famStart + 1}）\n`);

// ── 2. 基线（未注入）──
function probe(file) {
  const out = execFileSync('node', ['-e',
    `const hf=require(${JSON.stringify(file)});
     const ir=t=>((hf.discriminate(t).dimensions||{})['${DIM}']||{count:0}).count>0;
     console.log(JSON.stringify({
       atk: ${JSON.stringify(ATTACK)}.map(ir),
       ben: ${JSON.stringify(BENIGN)}.map(ir)
     }))`,
  ], { encoding: 'utf8' });
  return JSON.parse(out);
}

const base = probe(SRC);
const cnt = arr => arr.filter(Boolean).length;
console.log(`基线：攻击 ${cnt(base.atk)}/${ATTACK.length}，良性误伤 ${cnt(base.ben)}/${BENIGN.length}\n`);
if (cnt(base.atk) !== ATTACK.length || cnt(base.ben) !== 0) {
  console.error('基线异常，停止守卫');
  process.exit(1);
}

// ── 3. 逐条注入 ──
let realGuard = 0, backedUp = 0, broken = 0;
const detail = [];
for (const lineNo of famLines) {
  const mutated = [...lines];
  mutated[lineNo] = '';
  const tmp = path.join(__dirname, '..', 'src', `.tmp-ir-mutate-round59.js`);
  fs.writeFileSync(tmp, mutated.join('\n'));
  let r;
  try {
    r = probe(tmp);
  } catch (e) {
    detail.push(`行 ${lineNo + 1}: 注入后 require 失败/语法错 (${String(e.message).slice(0, 40)})`);
    broken++;
    continue;
  } finally {
    if (fs.existsSync(tmp)) fs.unlinkSync(tmp);
  }
  const lostAttack = r.atk.filter((hit, i) => base.atk[i] && !hit).length;
  const newFp = r.ben.filter((hit, i) => !base.ben[i] && hit).length;
  const snippet = (lines[lineNo].match(/,\s*'([a-z_]+)'\]\s*,\s*$/) || [])[1] || '(no type)';
  if (newFp > 0) {
    detail.push(`行 ${lineNo + 1} [${snippet}]: ⚠️ 注入后新增 ${newFp} 条良性误伤——判据本身有问题`);
    broken++;
  } else if (lostAttack > 0) {
    realGuard++;
    detail.push(`行 ${lineNo + 1} [${snippet}]: 真守卫（删后 ${lostAttack} 条攻击转漏判）`);
  } else {
    backedUp++;
    detail.push(`行 ${lineNo + 1} [${snippet}]: 有兜底（删后攻击仍全命中，被同族其他判据覆盖）`);
  }
}

console.log('═══ 逐条注入结果 ═══');
for (const d of detail) console.log('  ' + d);
console.log(`\n═══ 汇总：真守卫 ${realGuard} / 有兜底 ${backedUp} / 异常 ${broken} / 共 ${famLines.length} 条判据 ═══`);
if (broken > 0) process.exit(1);
